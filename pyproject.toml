[build-system]
requires = ["maturin>=1.4"]
build-backend = "maturin"

[project]
name = "harvest_rs"
version = "0.0.1"
requires-python = ">=3.8"
dependencies = ["numpy"]

[tool.maturin]
features = ["python"]


